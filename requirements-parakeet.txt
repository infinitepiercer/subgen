# Extra dependencies for the Parakeet (NeMo) ASR backend.
# Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-parakeet.txt
#
# NeMo pulls in PyTorch, torchaudio, etc. but those are already
# provided by the base Dockerfile layers, so the net download is
# mainly the NeMo framework, Cython decoders, and model weights.
nemo_toolkit[asr]>=2.0.0,<2.6.0
